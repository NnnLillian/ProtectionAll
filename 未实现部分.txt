1.添加team、team_department、team_category未实现，暂不知提交数据格式
2.生成报告时，返回整个scheme信息部分，不知前端接受形式，暂未实现

得到方位
(Integer)scheme_id
String combat_direction
select combat_direction from scheme_info where scheme_id = #{scheme_id}

环境特情
(string)case_position   输入
List<Special_Case>special_case_list  输出
select * from special_case_info where case_position = #{case_position }
保证资源情况
(string)case_position
List<Supplier>supplier_list
select * from supplier_info where case_position= #{case_position }


(Integer)team_id输入
List<Category>category_list输出
select category_info.category_name,category_info.category_unit, team_department_info.category_number
from team_category_info, team_department_info where team_id = #{team_id} and team_department_info.category_id = category_info.category_id

(Integer)team_id输入
List<Department>department_list输出
select department_info.department_name,team_department_info.department_people
from department_info,team_department_info where team_id = #{team_id} and department_info.department_id = team_department_info.department_id

装备特情
(Integer)scheme_id ,(string)case_position输入
List<Category_Case>category_case_list输出
selecte * from category_case_info where category_id in (select categroy_in from scheme_info where scheme_id = #{scheme_id}) and  case_position = #{case_position }
